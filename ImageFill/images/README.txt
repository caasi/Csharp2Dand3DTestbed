All images in this folder should have their 'Build' property set to "Resource". (Right click on the image file; select "Properties"; change
the "Build" property to "Resource.)

This will bundle them with the resulting executable, allowing them to be loaded regardless of where the executable is run. 
